# Verified builds

## Changed

- The libraries built into Proxor are now taken from fixed release archives and checked against a known checksum before they are compiled, so every build ships exactly the sources that were reviewed. Nothing changes in how the app looks or behaves.
- The release pipeline no longer depends on tooling that GitHub is retiring, so future updates keep arriving on every channel.
